/*
*  Count number of times C keywords appear in a file
*/

#define MAXWORD 100

int getword(char *, int );
int binsearch(char *, struct key *, int);

// Count C keywords
int main(int argc, char * argv[])
{
    
}

// Find word in keytab[0] ... keytab[n-1]
int binsearch(char * wrod, struct key tab[], int n)
{
    
}